{
	"type": "module",
	"workspaces": ["packages/*", "templates/*"],
	"scripts": {
		"build": "bun -F './packages/*' build",
		"watch": "bun --parallel -F './packages/*' watch",
		"typecheck": "bun -F './packages/*' typecheck",
		"lint": "bun -F './packages/*' lint",
		"test": "bun -F './packages/*' test"
	},
	"devDependencies": {
		"@kh4f/eslint-config": "^0.3.0",
		"@types/bun": "^1.4.0",
		"eslint": "^10.9.1",
		"tsdown": "0.22.14",
		"typescript": "^6.0.3"
	}
}